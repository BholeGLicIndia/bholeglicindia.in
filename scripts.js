document.addEventListener('DOMContentLoaded', () => {
    const navLinks = document.getElementById('nav-links');
    navLinks.classList.add('active'); // Ensure the nav menu is always displayed
});
